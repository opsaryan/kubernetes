apiVersion: v1 
kind: Service
metadata:
  name: nginx-nodeport
  labels:
    env: nginx-nodeport
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
  selector:
    type: nginx-pod