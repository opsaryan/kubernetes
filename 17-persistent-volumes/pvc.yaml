apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nginx-pvc
  labels:
    type: nginx-pvc
spec:
  storageClassName: ""
  accessModes:
    - ReadWriteMany
  selector:
    matchLabels:
      type: dev-pv
  resources:
    requests:
      storage: 500Mi