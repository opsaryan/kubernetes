apiVersion: v1 
kind: Namespace
metadata:
  name: ns1
  labels:
    type: ns-ns1

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-dpt
  namespace: ns1
  labels:
    type: nginx-dpt
spec:
  replicas: 2
  selector:
    matchLabels:
      type: nginx-pod
  template:
    metadata:
      name: nginx 
      labels:
        type: nginx-pod
    spec:
      containers:
        - name: nginx 
          image: nginx:latest
          ports:
            - containerPort: 80